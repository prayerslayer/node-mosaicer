/*

    # ImageStore

    - put( img, metadata )
    - query( color [, tags])
*/

class ImageStore {
    constructor() {

    }

    put( image, metadata ) {
        throw 'not implemented';
    }

    query( color, tags ) {
        throw 'not implemented';
    }
}

export default ImageStore;